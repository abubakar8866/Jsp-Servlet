require 3 jar files for servlet crud operation [mysql and servlet CRUD operations]
servlet-api.jar (apache-tomcat- --> lib folder)
tomcat-api.jar (apache-tomcat- --> lib folder)
mysql-connector-j-8.0.31.jar (independent file)
If Error, paste mysql-connector-j-8.0.31.jar file in lib folder (below location):
YourProject--> src-- > main--> webapp--> WEB--> INF--> lib

This project need mysql server.

It is Student crud project.
